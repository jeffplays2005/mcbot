module.exports.run = async (bot, username, unique, message, args, db, prefix) => {
  bot.chat('/chat p');
  bot.chat('§ehfu2hf29huf92eh')
}
module.exports.config = {
    // General
    name: "test",
    description: "test",
    usage: "test",
    aliases: [],
    disabled: false, // true/false
    hide: false,
}
